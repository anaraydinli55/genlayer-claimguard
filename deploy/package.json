{
  "name": "claimguard-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy:local": "ts-node deploy.ts",
    "deploy:testnet": "ts-node deploy.ts --testnet"
  },
  "dependencies": {
    "@genlayer/cli": "^0.1.0",
    "@genlayer/js": "^0.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.6.0"
  }
}
